# Ubuntu scripts

Ubuntu automation will live in this directory when Linux development resumes.

The planned scripts will cover source bootstrap, dependency preparation, compilation, packaging and smoke tests. They must consume the same shared SolidFreeCAD source and avoid copying platform-neutral behavior out of `src/`.

This directory is intentionally preserved while Windows is the current delivery priority.
